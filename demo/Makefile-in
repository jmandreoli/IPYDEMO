PYTHON :=
TARGET := fractals.out odesimu.out polyhedra.out sudoku.out

all: ${TARGET}

%.out: %.py
	@echo Generating $@ ...
	@${PYTHON} -Wignore make.py $(patsubst %.py,%,$<)

clean:
	@rm -f ${TARGET}
